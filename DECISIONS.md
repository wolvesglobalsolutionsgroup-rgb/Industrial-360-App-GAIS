# Registro de Decisiones de Arquitectura — Industrial Control 360

| Fecha | Decisión Tomada | Alternativas Consideradas | Razón de la Elección |
|---|---|---|---|
| 2026-07-26 | Sistema de Componentes UI Primitivos (`src/components/ui/`) para normalizar la UI | Mantener estilos Ad-hoc o instalar shadcn completo | Normaliza la interfaz con la estética "Industrial Executive", soportando densidad, border-radius y tokens de color dinámicos del `ThemeContext` sin sobrecargar bundle. |
| 2026-07-26 | Work Board Colaborativo Kanban sobre Firestore en lugar de Socket.io | Servidor WebSocket dedicado / Socket.io | Firestore brinda sincronización reactiva en tiempo real nativa, persistencia multi-tenant (`/tasks` por `projectId`) y soporte offline sin infraestructura adicional. |
| 2026-07-26 | `feat(pams-transversal): integrate PAMS modules, HotTap, network graph, code-splitting & Command Palette` | Mantener módulos separados o carga síncrona en bundle único de 4.2MB | Integración de PAMS (Hot Tap/PIT API 2201, Red D3 SCADA, Fases GPG/FEL V/C/D/I/O), Code-splitting con React.lazy() (suspense inicial de ~200KB), Vitest para ASME/API y Command Palette (⌘K) global. |
| 2026-07-26 | `style(theme): unify single theme system & real dark mode via CSS variables in @theme` | Mantener doble sistema de tema JS + CSS with presets parciales | Elimina la desincronización entre JS y CSS. Define un único bloque `@theme` en `src/index.css` con overrides `.dark` en `<html>` mediante `ThemeContext.tsx`, garantizando contraste dinámico instantáneo en toda la app. |
| 2026-08-06 | `arch(deliverables): canonical pdfExporter.ts & DocumentExporter contract for multi-format engine (Sprint F-E)` | Permitir imports directos de jsPDF dispersos en páginas y componentes | Centraliza jsPDF en `src/lib/pdfExporter.ts` como único punto de entrada. Desacopla la lógica visual mediante `DocumentViewModel` e implementa el contrato `DocumentExporter` para PDF, DOCX, XLSX y PPTX, reduciendo la deuda técnica de 13 a 1 import y garantizando testeo automatizado de inviolabilidad. |
